export interface InventoryModel {
  index: number;
  itemCode: string;
  itemName: string;
  unit: string;
  lotNumber: string;
  stockQuantity: number;
  positision: string;
  unitPrice: number;
  totalPrice: number;
}
