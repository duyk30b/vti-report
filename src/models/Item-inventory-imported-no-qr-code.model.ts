export interface ItemInventoryImportedNoQRCodeModel {
  index: number;
  orderCode: string;
  itemCode: string;
  itemName: string;
  unit: string;
  lotNumber: string;
  locationCode: string;
  actualQuantity: number;
  unitPrice: number;
  totalPrice: number;
}
